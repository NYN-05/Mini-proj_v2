# EduShield Architecture Overview

## 📊 High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         EduShield Application                    │
│                          (app_new.py)                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┬──────────────┐
        │                   │                   │              │
        ▼                   ▼                   ▼              ▼
┌───────────────┐  ┌────────────────┐  ┌──────────────┐  ┌──────────────┐
│  CORE MODULE  │  │ ANALYTICS      │  │   ADMIN      │  │  EDUCATION   │
│   (Primary)   │  │  MODULE        │  │   MODULE     │  │   MODULE     │
│               │  │ (Tertiary)     │  │ (Low-usage)  │  │(Supplementary)│
└───────────────┘  └────────────────┘  └──────────────┘  └──────────────┘
```

## 🔍 Detailed Component Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                            CONFIG LAYER                               │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  app/config.py                                                 │  │
│  │  • Paths (PROJECT_ROOT, MODELS_DIR, DB_PATH)                  │  │
│  │  • ML Config (TFIDF_CONFIG, MODEL_WEIGHTS)                    │  │
│  │  • Detection Config (CONFIDENCE_THRESHOLDS, DETECTION_WEIGHTS)│  │
│  │  • Feature Flags (FEATURES)                                   │  │
│  └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
                                    │
    ┌───────────────────────────────┼───────────────────────────────┐
    │                               │                               │
    ▼                               ▼                               ▼
┌─────────────────┐      ┌──────────────────────┐      ┌────────────────────┐
│  CORE MODULE    │      │  MODULES PACKAGE     │      │  TEMPLATES/STATIC  │
│  (app/core/)    │      │  (app/modules/)      │      │                    │
│                 │      │                      │      │  • HTML Templates  │
│  predictor.py   │      │  analytics/          │      │  • CSS Styles      │
│  ├─ ModelBundle │      │  ├─ database.py      │      │  • JavaScript      │
│  ├─ predict()   │      │  ├─ routes.py        │      │  • Images          │
│  └─ scoring     │      │  └─ __init__.py      │      │                    │
│                 │      │                      │      └────────────────────┘
│  explainer.py   │      │  admin/              │
│  ├─ highlight() │      │  ├─ routes.py        │
│  └─ risk_factors│      │  └─ __init__.py      │
│                 │      │                      │
│  routes.py      │      │  education/          │
│  ├─ /           │      │  ├─ tips.py          │
│  ├─ /detect     │      │  ├─ routes.py        │
│  └─ /detect/adv │      │  └─ __init__.py      │
└─────────────────┘      └──────────────────────┘
```

## 🔄 Request Flow

### Basic Detection Flow
```
User Browser
    │
    │ GET /
    ▼
┌─────────────────┐
│  core_bp (/)    │  ◄── Core Blueprint
└────────┬────────┘
         │ render_template('index.html')
         ▼
┌─────────────────┐
│  index.html     │  ◄── Main UI
└─────────────────┘
         │
         │ POST /detect {email_text}
         ▼
┌──────────────────────┐
│ core_bp (/detect)   │
│ ├─ Load ModelBundle │
│ ├─ predict_phishing │
│ ├─ highlight_keywords
│ └─ log_prediction   │  ──►  analytics.log_prediction()
└──────────────────────┘
         │
         │ JSON Response
         ▼
User Browser (Result Display)
```

### Analytics Dashboard Flow
```
Admin Browser
    │
    │ GET /admin
    ▼
┌──────────────────────┐
│ admin_bp (/admin)    │  ◄── Admin Blueprint
└────────┬─────────────┘
         │ render_template('admin_dashboard.html')
         ▼
┌──────────────────────┐
│ admin_dashboard.html │
│ (Dashboard UI)       │
└────────┬─────────────┘
         │
         │ Fetch Data via AJAX
         ▼
┌─────────────────────────────┐
│ analytics_bp                │  ◄── Analytics Blueprint
│ ├─ /api/analytics           │
│ ├─ /api/heatmap             │
│ └─ /api/user-awareness      │
└─────────┬───────────────────┘
          │
          ▼
┌──────────────────────┐
│ analytics.database   │  ◄── Database Layer
│ ├─ get_analytics_data│
│ ├─ get_attack_heatmap│
│ └─ get_user_awareness│
└──────────────────────┘
          │
          ▼
     SQLite Database
 (edushield_analytics.db)
```

## 🏗️ Layer Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                       │
│  • Flask Blueprints (routes.py)                            │
│  • HTML Templates (Jinja2)                                 │
│  • Static Assets (CSS, JS)                                 │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                     │
│  • Core Detection (predictor, explainer)                   │
│  • Advanced Detection (comprehensive_phishing_detection)   │
│  • Education Content (tips)                                │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                     DATA ACCESS LAYER                       │
│  • Analytics Database (SQLite)                             │
│  • Model Loading (joblib)                                  │
│  • Configuration (config.py)                               │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                      PERSISTENCE LAYER                      │
│  • models/ (ML models: .pkl files)                         │
│  • data/ (SQLite database)                                 │
│  • Configuration files                                     │
└────────────────────────────────────────────────────────────┘
```

## 🎨 Module Dependency Graph

```
                    ┌──────────────┐
                    │   config.py  │  ◄─── Central Configuration
                    └───────┬──────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌────────────────┐  ┌──────────────┐
│  core/        │  │  modules/      │  │  utils/      │
│               │  │  analytics/    │  │  (Legacy)    │
│  predictor ───┼──►  database      │  └──────────────┘
│  explainer    │  │                │
│  routes       │  │  admin/        │
└───────┬───────┘  │  routes        │
        │          │                │
        │          │  education/    │
        │          │  tips          │
        │          │  routes        │
        └──────────►                │
                   └────────────────┘

Legend:
  ──► Dependency (imports from)
  │   Hierarchical relationship
```

## 📦 Blueprint Registration Flow

```
app_new.py
    │
    ├─► create_app()
    │     │
    │     ├─► Flask(__name__)
    │     │
    │     ├─► init_db() ─────────────► analytics.database.init_db()
    │     │
    │     ├─► register_blueprint(core_bp)
    │     │        │
    │     │        ├─ Routes: /, /detect, /detect/advanced
    │     │
    │     ├─► register_blueprint(analytics_bp)
    │     │        │
    │     │        ├─ Routes: /api/analytics, /api/heatmap, /api/user-awareness
    │     │
    │     ├─► register_blueprint(admin_bp)
    │     │        │
    │     │        ├─ Routes: /admin
    │     │
    │     └─► register_blueprint(education_bp)
    │              │
    │              ├─ Routes: /api/security-tips
    │
    └─► app.run()
```

## 🔐 Security & Access Control

```
┌─────────────────────────────────────────────────────────┐
│                    Feature Flags                         │
│  (app/config.py: FEATURES)                              │
└───────────────────┬─────────────────────────────────────┘
                    │
        ┌───────────┼───────────┬──────────────┐
        │           │           │              │
        ▼           ▼           ▼              ▼
   analytics   admin_dash  advanced_det   education
     _enabled    _enabled     _enabled      _enabled
        │           │           │              │
        ▼           ▼           ▼              ▼
   ┌────────┐  ┌────────┐  ┌────────┐    ┌────────┐
   │ Check  │  │ Check  │  │ Check  │    │ Check  │
   │ in     │  │ in     │  │ in     │    │ in     │
   │ routes │  │ routes │  │ routes │    │ routes │
   └────────┘  └────────┘  └────────┘    └────────┘
        │           │           │              │
   [403 or OK] [403 or OK] [403 or OK]   [403 or OK]
```

## 📊 Data Flow: Prediction Logging

```
User Request (POST /detect)
    │
    ▼
┌────────────────────────┐
│ core.routes.detect()   │
│ ├─ predict_phishing()  │  ◄─── Core Detection
│ └─ Get: classification │
│         confidence     │
│         model_scores   │
└────────┬───────────────┘
         │
         │ If FEATURES['analytics_enabled']
         ▼
┌────────────────────────┐
│ analytics.log_prediction() ◄─── Analytics Module
│ ├─ INSERT INTO prediction_history
│ └─ Return success      │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ analytics.update_daily_statistics()
│ ├─ Aggregate today's predictions
│ └─ UPDATE daily_statistics
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ analytics.update_phishing_patterns()
│ ├─ Extract keywords    │
│ └─ UPDATE phishing_patterns
└────────────────────────┘
         │
         ▼
    SQLite Database
(6 analytics tables updated)
```

## 🎯 Key Benefits of This Architecture

```
┌──────────────────────────────────────────────────────────┐
│                  Separation of Concerns                   │
│  ┌────────────┐  ┌────────────┐  ┌───────────────────┐  │
│  │    Core    │  │  Analytics │  │  Admin/Education  │  │
│  │  Detection │  │   Logging  │  │    (Tertiary)     │  │
│  └────────────┘  └────────────┘  └───────────────────┘  │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                      Modularity                           │
│  • Independent testing per module                        │
│  • Can disable/enable features via flags                 │
│  • Easy to add new modules without touching core         │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                   Maintainability                         │
│  • Clear file organization                               │
│  • Centralized configuration                             │
│  • Blueprint-based routing                               │
│  • Reduced coupling between components                   │
└──────────────────────────────────────────────────────────┘
```

---

This architecture follows **SOLID principles** and **Clean Architecture** patterns for maximum maintainability and scalability.
