"""Utility wrappers for higher-level detection features.

This package provides compatibility wrappers so tests and higher-level
modules can import from `app.utils.*` even though core logic lives in
`app.core` and `app.modules`.
"""

__all__ = []
